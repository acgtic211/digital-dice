apiVersion: apps/v1
kind: Deployment
metadata:
  name: src-virtualizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: src-virtualizer
  template:
    metadata:
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/path: /metrics
        prometheus.io/port: '443'
      labels:
        app: src-virtualizer
    spec:
      containers:
      - name: src-virtualizer
        image: rrs999/digital-dice-virtualizer:0.1.0
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: 100m
        envFrom:
          - configMapRef:
              name: src-config
        volumeMounts:
          - name: tls-src
            mountPath: "/app/certs"
            readOnly: true
          - name: td-config
            mountPath: "/app/td"
            readOnly: true
          - name: behavior-config
            mountPath: "/app/behavior"
            readOnly: true
          - name: instances-config
            mountPath: "/app/instances"
            readOnly: true
      volumes:
        - name: tls-src
          secret:
            secretName: tls-src
        - name: td-config
          configMap:
            name: td-config
        - name: behavior-config
          configMap:
            name: behavior-config
        - name: instances-config
          configMap:
            name: instances-config
---
apiVersion: v1
kind: Service
metadata:
  name: src-virtualizer-entrypoint
spec:
  selector:
    app: src-virtualizer
  ports:
  - protocol: TCP
    port: 8065
    targetPort: 8065
    nodePort: 30002
  type: NodePort